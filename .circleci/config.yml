version: 2.1
jobs:
  print_hello:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: echo Hello
          
          
  print_jambo:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: echo Jambo

  jina_langu:
    docker: 
      - image: cimg/base:2020.01
    steps:
      - checkout
      - run: 
        name: " echo a name var stored in the project enviroment"
        command: |
        echo $MY_ENV_VAR

workflows:
  welcome:
    jobs:
      - print_hello
      - jina_langu
      - print_jambo:
          requires:
            - print_hello
